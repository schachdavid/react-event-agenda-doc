import React from "react";
import { markdown, ReactSpecimen } from "catalog";
import ExampleComponent from 'react-event-agenda'


export default () =>
  markdown`
> Learn about the usage and architecture of react-event-agenda

react-event-agenda exposes a view model. By calling the viewmodel's methods the data of the agenda can be manipulated.

  `;

